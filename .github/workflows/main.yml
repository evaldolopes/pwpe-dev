name: "🚀 Deploy website on MAIN"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  # workflow_dispatch:

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: portalwebpe.com.br
          username: pwpecombr
          password: K!ko3009
          local-dir: ./src/
          server-dir: /dev.portalwebpe.com.br/
